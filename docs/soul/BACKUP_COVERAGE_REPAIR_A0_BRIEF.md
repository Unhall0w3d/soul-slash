# Backup coverage repair A0

The Operator requested repair of the September 4 coverage audit findings.
This candidate adds explicit Soul source/documentation roots to portable policy,
and previews the already-supported Operator Scripts source. It must preserve
live manifest entries, review gates, permissions, and existing exclusions.
No whole project/home root, privileged reader, service, snapshot, or automatic
capture is authorized by this implementation brief. Restricted system capture
remains a separate reviewed design. Manifest inclusion is not backup proof.

Acceptance: existing reconciliation verifier passes; explicit source roots are
selected in fixtures; whole project root is absent; symlink sources remain
excluded; no live manifests are written during candidate preparation.
